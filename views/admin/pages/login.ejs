<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Roboto', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(45deg, #007bff, #00c3ff);
            color: #333;
        }
        .container {
            display: flex;
            width: 80%;
            max-width: 600px;
            background-color: #fff;
            box-shadow: 0px 15px 30px rgba(0, 0, 0, 0.2);
            border-radius: 15px;
            overflow: hidden;
            transform: scale(1);
            transition: all 0.3s ease;
        }
        .container:hover {
            transform: scale(1.02);
        }
        /* Admin Login Form */
        .admin-pane {
            flex: 1;
            padding: 60px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            background: #f7f7f7;
        }
        .admin-pane h2 {
            margin-bottom: 20px;
            font-weight: 700;
            font-size: 28px;
            color: #444;
            text-align: center;
        }
        .admin-pane form {
            display: flex;
            flex-direction: column;
        }
        .admin-pane input {
            padding: 15px;
            margin-bottom: 20px;
            font-size: 16px;
            border-radius: 8px;
            border: 1px solid #ddd;
            background-color: #fff;
            transition: border-color 0.3s ease;
        }
        .admin-pane input:focus {
            border-color: #007bff;
            outline: none;
        }
        .admin-pane button {
            padding: 15px;
            font-size: 18px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            font-weight: bold;
            margin-bottom: 15px;
        }
        .admin-pane button:hover {
            background-color: #0056b3;
        }
        .admin-pane a {
            color: #007bff;
            text-decoration: none;
            font-size: 14px;
            margin-top: 10px;
            text-align: center;
            transition: color 0.3s ease;
        }
        .admin-pane a:hover {
            color: #0056b3;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Admin Login Pane -->
        <div class="admin-pane">
            <h2>Admin Login</h2>
            <form action="/admin-login" method="POST">
                <input type="text" name="username" placeholder="Admin Username" required>
                <input type="password" name="password" placeholder="Password" required>
                <button type="submit">Login</button>
            </form>
            <button onclick="window.location.href='/login'" style="background-color: #FF6F61;">Back to Customer Login</button>
        </div>
    </div>
</body>
</html>
